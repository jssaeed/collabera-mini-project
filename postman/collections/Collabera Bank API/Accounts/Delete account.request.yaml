$kind: http-request
url: "{{baseUrl}}/accounts/{{createdAccountId}}/"
method: DELETE
headers:
  Authorization: Bearer {{accessToken}}
scripts:
  - type: afterResponse
    code: pm.test('status is 204', () => pm.response.to.have.status(204));
    language: text/javascript
order: 8000
