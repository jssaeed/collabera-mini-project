$kind: http-request
url: "{{baseUrl}}/users/"
method: GET
headers:
  Authorization: Bearer {{accessToken}}
scripts:
  - type: afterResponse
    code: pm.test('status is 403 Forbidden', () => pm.response.to.have.status(403));
    language: text/javascript
order: 2000
