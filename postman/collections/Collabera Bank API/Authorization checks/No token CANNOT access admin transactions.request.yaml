$kind: http-request
url: "{{baseUrl}}/transactions/"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is 401 Unauthorized (no credentials at all)', () => pm.response.to.have.status(401));
    language: text/javascript
order: 3000
